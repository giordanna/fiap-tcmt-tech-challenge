# Variáveis de ambiente para Cloud Run (GCP)
# Este arquivo é apenas para documentação - as variáveis reais são configuradas via Terraform

# Conexão com Cloud SQL (Unix socket)
DB_HOST=/cloudsql/PROJECT_ID:southamerica-east1:INSTANCE_NAME
DB_USER=postgres
DB_NAME=postgres
DB_PASSWORD=(gerenciado pelo Secret Manager - não definir aqui)

# Configuração da API
API_PORT=8080

# Notas:
# - DB_HOST usa Unix socket no formato /cloudsql/PROJECT:REGION:INSTANCE
# - DB_PASSWORD é injetado automaticamente pelo Secret Manager
# - Estas variáveis são configuradas no Terraform (infra/main.tf)
